/**
 * @format
 */

import {AppRegistry} from 'react-native';
import ParkingApp from './src/ParkingApp';
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => ParkingApp);
